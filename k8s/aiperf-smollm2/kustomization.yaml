apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../aiperf
patches:
  - target:
      kind: Job
      name: aiperf
    patch: |-
      - op: replace
        path: /metadata/name
        value: aiperf-smollm2
